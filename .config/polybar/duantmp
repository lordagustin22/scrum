
[module/xwindow]
type = internal/xwindow
; https://github.com/polybar/polybar/blob/master/src/modules/xwindow.cpp
; Available tags:
   <label> (default)
format = <label>
format-background = #f00
format-foreground = #000
format-padding = 4

; Available tokens:
;   %title%
; Default: %title%
; label = %title%
; label-maxlen = 50

; Used instead of label when there is no window title
; Available tokens:
;   None
label-empty = Empty
label-empty-foreground = #707880

format-foreground = ${colors.foreground}
format-background = ${colors.background}
