# The simple hotkey daemon
# config

# Kill a process

# kill dwm
 # super + shift + q
#   pkill -SIGUSR1 dwm

# kill
# reboots sxhkd
alt + {Escape,m,4}
	{pkill -SIGUSR1 sxhkd,pkill -USR1 mpd && mpd,pkill -USR1 screenkey}

# open GUI
alt + {a,b,v,x,g,
    {anki,brave,pavucontrol,galculator,gimp}

# open CLI
# alt + {d,
alt + {s,w,h,t,z,l}
    {screenkey,st -e ncmpcpp,st -e htop,transmission-daemon && st -e tremc,st -e calcurse,st -e ~/.local/bin/lfub}
   # {dmenu_run -i -l 10,

# take screenshot
Print
    ~/.local/bin/maimpick.sh

# record or kill
super + Print
    ~/.local/bin/dmenurecord

super + alt + Print
    ~/.local/bin/killrecording

# dunstctl
# close
ctrl + space
    dunstctl close

# close all
alt + ctrl + space
    dunstctl close-all

# history menu
ctrl + period
    dunstctl history-pop

# context menu
alt + ctrl + period
    dunstctl context

# Mount/Unmount devices
# mount android device
alt + {2,3}
    {go-mtpfs ~/cell,fusermount -u cell/}

# juga do juejito nwn
alt + 5
    ~/nupominwn
