# The simple hotkey daemon
# config

# Kill a process

# kill
# reboots sxhkd
alt + {Escape,m,4,z}
	{pkill -USR1 -x sxhkd,pkill -USR1 mpd && mpd,pkill -USR1 screenkey, kill -USR1 $(pidof obs)}

# open GUI
alt + {a,b,v,g,o,d,n}
    {anki,$BROWSER,pavucontrol,gimp,virt-manager,obs,appimage_run}

# open CLI
alt + {s,w,h,t,q,l}
    {screenkey,$DTERM ncmpcpp,$DTERM htop,$DTERM tremc,$DTERM calcurse,$DTERM lfub}

# open terminal itself
super + Return
    $TERMINAL

# open rofi
super + @space
    rofrun

# open calculator
XF86Calculator
    galculator

# take screenshoot
Print
    maimpick

# dunst control
# close topmost notification
ctrl + space
    dunstctl close

# close all notifications currently being displayed
ctrl + alt + space
    dunstctl close-all

ctrl + period
    dunstctl history-pop
